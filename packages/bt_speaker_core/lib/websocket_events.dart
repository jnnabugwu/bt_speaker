///We use these constants to identify the type of event
///that we are sending or receiving from the websocket
const String kBeatEvent = 'beat';

///The type of event for the led
const String kLedEvent = 'led';

///The type of event for the equalizer
const String kEqEvent = 'eq';

///The type of event for the now playing song
const String kNowPlaying = 'now_playing';
